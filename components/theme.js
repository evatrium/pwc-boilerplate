
export let theme = {
    zIndex: {
        nav: 7000,
    },
    shadow: [
        '0px 4px 19px 1px rgba(150, 160, 229, 0.18)'
    ],
    spacing: 20,
    navHeight: 56,
    resets: jcss`:host, *, *::before, *::after { box-sizing: border-box; }`,
};